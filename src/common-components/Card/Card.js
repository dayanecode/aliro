import styled from 'styled-components';
import { BorderRadiuses, Spaces } from '../../shared/DesignTokens';

export const Card = styled.div`
	width: 100%;
	height: 100%;
    padding: ${BorderRadiuses.FOUR};
    border-radius: ${BorderRadiuses.ONE};

`;
    